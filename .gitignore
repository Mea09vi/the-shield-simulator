# --- Local caches & generated data ---
data/dem/cache/
data/dem/_land_elev_compact.json
_bathy/
scripts/tools/_woa_cache/
scripts/_inject_v10.py

# --- Local dev / preview config ---
.claude/

# --- Backups / old versions ---
_เวอร์ชันเก่า/
*Copy*.html
*.bak
*.bak_*
*.before-embed.bak

# --- Local raw data dumps & scratch mockups (NEVER commit — large / WIP) ---
_data_raw/
_scratch/
Sim2_UI_Mockup.html
_RTN_theme_preview.html

# --- Appendix / large binaries we don't want in git ---
*.docx
# Research/source PDFs and ALL account/credential PDFs stay LOCAL only.
*.pdf
# Intermediate silent master for the pitch reel (build artifact — only film.mp4 ships)
*.silent.mp4
ภาคผนวก_Viewer.html
ภาคผนวก_Viewer.html.before-embed.bak

# --- Misc ---
searches-export-search-results-*.json
scripts/generate_appendix.py
scripts/fetch_land_elevation.py

# --- OS / editor ---
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# --- Local credentials / account files (NEVER commit) ---
*Account*.pdf
RTN*Account*.pdf
*credentials*.json
*.env

# --- Cloudflare Wrangler local cache (account info — NEVER commit) ---
.wrangler/

# --- Folder reorg (2026-07): local dev-server scripts moved out of repo root ---
#     (proxy_server.py / ocean4d_live_server.js / start-*.bat / *-static.js — LOCAL dev only)
scripts/server/

# --- UI backups & graph build output (NEVER commit — large / WIP) ---
_backup_ui/
graphify-out/

# --- Pitch / presentation / workshop docs (kept local, not part of the simulator repo) ---
presentation.html
rtn_emblem.png
THE_SHIELD_Pitch_Script.md
THE_SHIELD_Pitch_Script_v2.md
THE_SHIELD_Defense_Script_15min.md
THE_SHIELD_Workshop_ClaudeCode.html

# --- Folder-organization meta notes (local) ---
FOLDER_ORGANIZATION.md
ORGANIZATION_ASSESSMENT.md
