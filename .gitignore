# --- Local caches & generated data ---
data/dem/cache/
data/dem/_land_elev_compact.json
_bathy/
scripts/tools/_woa_cache/
scripts/_inject_v10.py

# --- Local dev / preview config ---
.claude/

# --- Backups / old versions ---
_เวอร์ชันเก่า/
*Copy*.html
*.bak
*.bak_*
*.before-embed.bak

# --- Local raw data dumps & scratch mockups (NEVER commit — large / WIP) ---
_data_raw/
Sim2_UI_Mockup.html
_RTN_theme_preview.html

# --- Appendix / large binaries we don't want in git ---
*.docx
ภาคผนวก_Viewer.html
ภาคผนวก_Viewer.html.before-embed.bak

# --- Misc ---
searches-export-search-results-*.json
scripts/generate_appendix.py
scripts/fetch_land_elevation.py

# --- OS / editor ---
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# --- Local credentials / account files (NEVER commit) ---
*Account*.pdf
RTN*Account*.pdf
*credentials*.json
*.env

# --- Cloudflare Wrangler local cache (account info — NEVER commit) ---
.wrangler/
